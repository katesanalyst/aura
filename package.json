{
  "name": "aura-ui",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter template dev",
    "build": "pnpm --filter ui run typecheck && pnpm --filter template build",
    "build:prod": "pnpm --filter ui run typecheck && pnpm --filter template build",
    "preview": "pnpm --filter template start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "next-pwa": "^5.6.0"
  }
}
